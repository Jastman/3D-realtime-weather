# Google Maps Platform API key (enable "Maps Tile API" in Cloud Console)
# Without this, ESRI World Imagery satellite tiles are used as fallback (looks the same)
NEXT_PUBLIC_GOOGLE_MAPS_KEY=

# Optional: Mapbox token for terrain elevation tiles
# https://account.mapbox.com/access-tokens/
NEXT_PUBLIC_MAPBOX_TOKEN=
